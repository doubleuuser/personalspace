<meta content="text/html; charset=UTF-8" name="Content-Type"/>
<%= display_meta_tags og: { title: @post.title, description: @post.description, image: @post.image } %>
<%= display_meta_tags twitter: { title: @post.title, description: @post.description, image: @post.image, card: "summary" } %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">
        <div class="background-home">
          <%# <div class="card-product-show">
          <div><%= link_to @post.original_url do %>
          <div class="card-trip"><%= link_to @post.original_url do %>
                  <% if post.image? %>
                  <%= image_tag(post.image) %>
                  <% end %>
                  <% end %>
            <div class="card-trip-infos">
                <div>
                  <h2><%= post.title.truncate(100) %></h2>
                  <p><%= post.note %></p>
                </div>
                  <h2 class="card-trip-pricing">£99.99</h2>
                  <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
              </div>
          </div><% end %>
        </div>
      </div>
  </div>
</div>
